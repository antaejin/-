#include <stdio.h> // 성적표기

int main() {
	 
	int a;
	scanf_s("%d", &a);

	if (a > 100 || a < 0) { printf("잘못입력 하셨습니다.\n"); }
	else { 
		if (a >= 90) { printf("A\n"); }  // 조건이 하나일땐 중가로 생략가능
		else if (a >= 80) { printf("B\n"); }
		else if (a >= 70) { printf("C\n"); }
		else if (a >= 60) { printf("D\n"); }
		else { printf("F\n"); }
		}
}



#include <stdio.h> // 자연수 표기 

int main() {

	int a;
	scanf_s("%d", &a);

	for (int s = 1; s <= a; s++) {
		if (a % s == 0) {
			printf("%d ", s);
		 }
	}
}


#include <stdio.h> // 3 6 9

int main() {

	int g;
	scanf_s("%d", &g);

	for (int a = 1; a <= g; a++) {
		int z = a % 10;
		if (z == 3 || z == 6 || z == 9) {
			printf("* ");
		}
		else {
			printf("%d ", a);
		}
	}
}
